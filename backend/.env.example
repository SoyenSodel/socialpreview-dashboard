# Server Configuration
HOST=0.0.0.0
PORT=3000

# Database
# Use absolute path for production!
DATABASE_URL=sqlite:/absolute/path/to/socialpreview.db?mode=rwc

# JWT Secret
# CRITICAL: Generate a secure random secret using: openssl rand -base64 64
# NEVER use the example value below in production!
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_SECRET_MINIMUM_256_BITS

# Registration Secret
# Generate with: openssl rand -base64 32
# Keep this private! Only use it when registering new users
REGISTRATION_SECRET=REPLACE_WITH_SECURE_RANDOM_SECRET

# CORS - Frontend URL
# For production: https://panel.socialpreview.cz (backend serves frontend)
# For development: http://localhost:5173 (separate Vite dev server)
FRONTEND_URL=https://panel.socialpreview.cz

# Logging
# Use 'info' or 'warn' for production, 'debug' for development
RUST_LOG=info,socialpreview_backend=info,tower_http=info
